#
# Configuration file for OSM import
#

# Uncomment to avoid laundering of keys ( ':' turned into '_' )
attribute_name_laundering=no
# uncomment to report all ways, including the ones without any (significant) tag
report_all_ways=yes
# Valid values for tags_format are "hstore" and "json"
tags_format=hstore


[points]
# common attributes
osm_id=yes
osm_version=no
osm_timestamp=no
osm_uid=no
osm_user=no
osm_changeset=no

# keys to report as OGR fields
attributes=name,amenity,operator,operator:type,capacity,grades,isced:level,min_age,max_age,fee,ref
# keys that should NOT be reported in the "other_tags" field
ignore=created_by,converted_by,source,time,ele,note,todo,openGeoDB:,fixme,FIXME

[lines]
# common attributes
osm_id=yes
osm_version=no
osm_timestamp=no
osm_uid=no
osm_user=no
osm_changeset=no

# keys to report as OGR fields
attributes=name,amenity,operator,operator:type,capacity,grades,isced:level,min_age,max_age,fee,ref
# keys that should NOT be reported in the "other_tags" field
ignore=created_by,converted_by,source,time,ele,note,todo,openGeoDB:,fixme,FIXME

[multipolygons]
# common attributes
osm_id=yes
osm_version=no
osm_timestamp=no
osm_uid=no
osm_user=no
osm_changeset=no

# keys to report as OGR fields
attributes=name,amenity,operator,operator:type,capacity,grades,isced:level,min_age,max_age,fee,ref
# keys that should NOT be reported in the "other_tags" field
ignore=created_by,converted_by,source,time,ele,note,todo,openGeoDB:,fixme,FIXME
